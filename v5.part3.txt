fit>0)state.business+=Math.round(profit*(.48+Math.min(10,state.skill)*.028));if(state.route==='resale'&&profit>0)state.business+=Math.round(profit*.14);if(state.route==='service'&&profit>0)state.business+=Math.round(profit*.24);if(state.route==='investment')state.investment=Math.max(0,Math.round(state.investment*(1+.012+(rnd()-.5)*.14-state.risk*.0004)));state.stress=Math.max(0,Math.min(120,state.stress+(profit<0?6:-2)+(incident?5:0)));state.month+=3+Math.floor(rnd()*2);while(state.month>12){state.year++;state.month-=12}return {sales,profit,incident}}
function status(){const n=net(),delta=state.lastDelta||0,p=Math.min(100,n/GOAL*100),tag=n>=GOAL?'基业已成':p>=60?'大幅领先':p>=30?'领先':p>=12?'正常':p>=5?'落后':'危险';document.querySelector('#progress').innerHTML=`<div class="row"><span>净资产 <b>¥${n.toLocaleString('zh-CN')}</b> / ¥1,000,000</span><b>${p.toFixed(1)}%</b></div><div class="bar"><i style="width:${p}%"></i></div><small>近阶段：${delta>=0?'上涨':'下降'} ${Math.abs(delta).toLocaleString('zh-CN')} ｜经营状态：${tag}</small>`;document.querySelector('#year').textContent=`第 ${Math.min(10,state.year)} 年 · ${state.month} 月`;document.querySelector('#chapter').textContent=chapters[Math.min(4,Math.floor((state.year-1)/2))]}
function save(){localStorage.setItem(KEY,JSON.stringify(state))}
function render(){if(state.year>10||state.turn>=34)return ending();status();if(state.stage==='result'){document.querySelector('#story').innerHTML=`<article class="card after"><span class="label">选择后的几个月</span>${state.pending}</article>`;document.querySelector('#choices').innerHTML='<button id="continue" class="choice"><b>继续经营</b><span>时间会推着临川向前；今天的选择也会在未来留下新的影响。</span></button>';document.querySelector('#continue').onclick=()=>{state.stage='event';state.pending='';save();render()};return}const e=state.current?events.find(x=>x.id===state.current):pick();state.current=e.id;document.querySelector('#story').innerHTML=`<article class="card"><span class="label">事件 · ${e.title}</span>${e.seed}</article>`;document.querySelector('#choices').innerHTML=e.opts.map((o,i)=>`<button class="choice" data-i="${i}"><b>${o.label}</b><span>${o.detail}</span></button>`).join('');document.querySelectorAll('.choice').forEach(b=>b.onclick=()=>choose(e,+b.dataset.i));save()}
function updateRelations(e,o){const delta=Math.max(-6,Math.min(6,(o.effect.credit||0)+(o.effect.stress<0?1:0)));let who='';if(/周姐|房东|住处|租金/.test(e.title))who='zhou';else if(/阿杰|供应商|竞价|北桥/.test(e.title))who='ajie';else if(/苏岚|平台|店铺|流量/.test(e.title))who='sulan';else if(/许宁|投资|还款|税务|账/.test(e.title))who='xuning';else if(/^w\d+$/.test(e.id))who=['zhou','ajie','sulan','xuning'][Number(e.id.slice(1))%4];if(who)state.relations[who]=Math.max(0,Math.min(100,state.relations[who]+delta))}
function choose(e,i){const before=net(),o=e.opts[i];state.current='';add(o.effect);updateRelations(e,o);state.seen.push(e.id);const eventTime=`第${state.year}年${state.month}月`;const s=settle();const after=net(),delta=after-before;state.lastNet=after;state.lastDelta=delta;state.maxNet=Math.max(state.maxNet,after);state.minNet=Math.min(state.minNet,after);state.bestGain=Math.max(state.bestGain,delta);state.worstLoss=Math.min(state.worstLoss,delta);state.history.push({year:state.year,month:state.month,time:eventTime,event:e.title,choice:o.label,result:delta});let result=`${o.after}\n\n接下来的几个月，经营没有停在这一刻。订单、房租、工资、售后和生活照常推进。${s.profit>=0?'回款扣除日常支出后，账上终于多出一点可继续选择的余量。':'回款没有覆盖这段时间的全部支出，你不得不暂停一项原本计划好的安排。'}${s.incident?' 期间又发生一笔计划外损失，原本宽松的安排重新变紧。':''}\n\n${state.delays.length?'你还不知道，今天留下的某个细节已经写进未来的评分、关系或现金流。':''}`;if(state.stress>=100&&!state.seen.includes('burnoutEcho')){add({cash:-2200,stress:-45,route:'recovery'});state.delays.push({id:'burnoutEcho',at:state.turn});state.recoveries++;result+='\n\n长期透支超过极限。你不得不暂停经营，处理生活、休息、违约与旧售后。这不是结局，却让恢复成为一段真实的新剧情。'}state.pending=result;state.stage='result';state.turn++;save();render()}
function ending(){const n=net(),p=Math.min(100,n/GOAL*100),route={resale:'渠道周转',ecommerce:'店铺系统',investment:'独立投资',recovery:'重整生存',service:'服务与撮合',start:'稳健起步'}[state.route]||'混合经营',grade=n>=GOAL?'百万经营者':state.recoveries?'低谷重建者':state.risk>58?'高波动冒险者':state.credit>68?'关系型稳健经营者':state.business>180000?'系统扩张者':'谨慎积累者',names={zhou:'周姐',ajie:'阿杰',sulan:'苏岚',xuning:'许宁'},bestFriend=Object.entries(state.relations).sort((a,b)=>b[1]-a[1])[0],best=state.history.reduce((a,b)=>b.result>a.result?b:a,state.history[0]||{event:'雨夜的临川',choice:'尚未作出选择',result:0}),worst=state.history.reduce((a,b)=>b.result<a.result?b:a,state.history[0]||{event:'雨夜的临川',choice:'尚未作出选择',result:0}),crises=state.history.filter(h=>/退款|限流|差评|还款|漏水|停摆|伤|侵权|丢件|退货/.test(h.event)).length;document.querySelector('#progress').innerHTML=`<div class="row"><span>十年目标完成度</span><b>${p.toFixed(1)}%</b></div><div class="bar"><i style="width:${p}%"></i></div><small>最终净资产 ¥${n.toLocaleString('zh-CN')}</small>`;document.querySelector('#year').textContent='十年经营报告';document.querySelector('#chapter').textContent=grade;document.querySelector('#story').innerHTML=`<article class="card"><span class="label">${state.name} 的十年经营报告</span>最终净资产：¥${n.toLocaleString('zh-CN')}\n目标完成度：${p.toFixed(1)}%\n最高净资产：¥${state.maxNet.toLocaleString('zh-CN')}\n最低谷：¥${state.minNet.toLocaleString('zh-CN')}\n主要路线：${route}\n经历事件：${state.history.length} / ${events.length}\n经历危机：${crises} 次\n进入恢复期：${state.recoveries} 次\n关系最深的人：${names[bestFriend[0]]}\n\n最大一次增长\n${best.event}｜${best.choice}\n净资产阶段变化：${best.result>=0?'+':''}¥${best.result.toLocaleString('zh-CN')}\n\n代价最大的一次决定\n${worst.event}｜${worst.choice}\n净资产阶段变化：${worst.result>=0?'+':''}¥${worst.result.toLocaleString('zh-CN')}\n\n你的经营类型：${grade}\n\n${n>=GOAL?'你达到了一百万元。真正把你带到这里的，不是某一次押注，而是十年里对现金、信用、关系与生活边界的反复选择。':'你没有在十年内达到一百万元，但故事没有把失败写成简单扣分。你走过的低谷、转向的服务路线、仍愿意联系你的人，以及学会停止透支的时刻，都构成下一段经营的起点。'}</article>`;document.querySelector('#choices').innerHTML='<button id="again" class="choice"><b>重新开始另一条十年</b><span>选择不同的起步路线，将遇见不同的人物节点、失败出口与生活事件。</span></button>';document.querySelector('#again').onclick=()=>{localStorage.removeItem(KEY);state=null;document.querySelector('#name').value='';document.querySelector('#start').showModal()};save()}
const dialog=document.querySelector('#start');if(!state)dialog.showModal();else render();document.querySelector('#begin').onclick=()=>{state=fresh(document.querySelector('#name').value.trim());render()};document.querySelector('#restart').onclick=()=>{localStorage.removeItem(KEY);state=null;document.querySelector('#name').value='';dialog.showModal()};
